curl -L http://127.0.0.1:4444/join/%3A9019

curl -L http://127.0.0.1:4444/ -X PUT -d '{"Key":"aa", "Value":"aa"}'
curl -L http://127.0.0.1:4444/ -X PUT -d '{"Key":"ab", "Value":"ab"}'
curl -L http://127.0.0.1:4444/ -X PUT -d '{"Key":"ddd", "Value":"ddd"}'
curl -L http://127.0.0.1:4444/ -X PUT -d '{"Key":"dde", "Value":"dde"}'

curl -L http://127.0.0.1:4444/ -X PUT -d '{"Key":"abc", "Value":"abc"}'
curl -L http://127.0.0.1:4444/ -X PUT -d '{"Key":"abd", "Value":"abd"}'


EXECUTION COMMANDS

* Delete temp folder after each execution
* Wait for 2 secs after execting KeyValue


./KeyValue -state_dir=./TEMP/1 -raft :8080 -api :9000 -shrdctlraddr :4444 -group_id 100
./KeyValue -state_dir=./TEMP/2 -raft :8081 -api :9010 -shrdctlraddr :4444 -group_id 100 -join :8080
./KeyValue -state_dir=./TEMP/3 -raft :8082 -api :9020 -shrdctlraddr :4444 -group_id 100 -join :8080

./KeyValue -state_dir=./TEMP/4 -raft :8090 -api :9090 -shrdctlraddr :4444 -group_id 300

go run main.go :4444 100:9000 300:9090

curl -L http://127.0.0.1:4444/ -X PUT -d '{"Key":"aa", "Value":"aa"}' 
curl -L http://127.0.0.1:4444/ -X PUT -d '{"Key":"ab", "Value":"ab"}'
curl -L http://127.0.0.1:4444/ -X PUT -d '{"Key":"ddd", "Value":"ddd"}'
curl -L http://127.0.0.1:4444/ -X PUT -d '{"Key":"dde", "Value":"dde"}'

curl -L http://127.0.0.1:9020/mgmt/nodes 

* select the raft id of node you want to delete (node with 9090) *
* and send the request to deletenode to that node which you know will become leader *
* in this case, 9010 *

curl -L http://127.0.0.1:9010/mgmt/nodes/1204342086345470985 -X DELETE

* select the raft id of node you want to delete (node with 9010) *
* and send the request to deletenode to that node which you know will become leader *
* in this case, 9020 *

curl -L http://127.0.0.1:9020/mgmt/nodes/7897747123307142415 -X DELETE

* verify whether cluster is removed, from shardcontroller *


* join command *

curl -L http://127.0.0.1:4444/join/200/9019 

* leave command *

curl -L http://127.0.0.1:4444/leave/200/ 